<h1 class="page-header">
	<img src="{{ $viewModel->pageDetails->language->logo_path }}" alt="" height="60">
    {{ $viewModel->pageDetails->name }}
    <small>{{ $viewModel->pageDetails->title }}</small>
</h1>